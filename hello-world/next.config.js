module.exports = {
  /* React strict mode is a development mode only feature for highlighting potential problems in an application
  it helps identify unsafe life cycles legacy api usage and a lot more
  */
  reactStrictMode: true,
}
